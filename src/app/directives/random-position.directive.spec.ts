import { RandomPositionDirective } from './random-position.directive';

describe('RandomPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomPositionDirective();
    expect(directive).toBeTruthy();
  });
});
